version: "3.8"

services:
  app:
    build: .
    ports:
      - "8081:8081"
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER:neondb_owner
      POSTGRES_PASSWORD: npg_wifF4jSX8BrW
      POSTGRES_DB:jdbc:postgresql://ep-super-feather-ab4y0cbx-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require&channelBinding=require
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
